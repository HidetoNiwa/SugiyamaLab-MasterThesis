name: CI
on:
    push:
        branches:
        - develop/#5-github-actions
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Set up Git repository
        uses: actions/checkout@v1
        env:
          TZ: 'Asia/Tokyo'
      - name: Build docker image
        uses: HidetoNiwa/SugiyamaLab-MasterThesis/.github/actions/latex@develop/#5-github-actions
        env:
          TZ: 'Asia/Tokyo'
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
